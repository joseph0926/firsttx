---
'@firsttx/prepaint': patch
'@firsttx/shared': patch
---

Restrict sanitized data URLs to valid base64 PNG, JPEG, GIF, WebP, and AVIF images, and block obfuscated JavaScript and VBScript URL schemes.
